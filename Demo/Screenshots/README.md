# Screenshots

Empty on purpose. When this repo was produced, Xcode on the build machine already had an
unrelated production project open, and the pipeline's rule for that case is to stop rather
than click through someone else's work. No Simulator run happened, so there is no capture
to put here — and nothing here is a diagram relabelled as one.

Everything the library claims is pinned by `swift test` (19 tests), which needs no Xcode.
When a real run produces real captures, they go here.
